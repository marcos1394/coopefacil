// src/components/Payments.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const Payments = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Registro de Pagos
      </Typography>
      {/* Aquí irá el formulario de registro de pagos */}
    </Box>
  );
};

export default Payments;
